name: "build"
on:
    push:
        branches:
            - "master"
            - "main"
        paths-ignore:
            - "**/*.md"
    pull_request:
        paths-ignore:
            - "**/*.md"
    workflow_dispatch: ~

jobs:
    build:
        uses: "Setono/SyliusPluginSkeleton/.github/workflows/reusable-build.yaml@2.2.x"
        secrets: "inherit"
